
name: Create and publish a Docker image

on:
  push:
    branches: 
      - main

jobs:
  run-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
      - uses: actions/checkout@v2
      - name: Set-up Node
      - uses: actions/setup-node@v1
      - name: Running install, prettier and deploy
      - run: npm install
      - run: npm run prettier
      - run: npm run deploy
